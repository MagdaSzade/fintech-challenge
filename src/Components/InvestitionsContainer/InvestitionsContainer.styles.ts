import {css} from 'emotion';
import {cssColors} from '../../helpers/cssConsants';

export const investitionsContainerStyles = css({
    backgroundColor: cssColors.background,
    borderRadius: '20px',
    width: '80%',
    margin: '0 auto',
});
